function sum(num, num2){
    return num + num2;
}

let test = sum(5, 12);

//Basic function utilizing numbers

console.log(test)

function str(a, b){
    let strs = `${a} ${b}`;
    return strs; 
}

const str1 = 'Well hello there.';
const str2 = 'May I know your name?';

console.log(str(str1, str2));

//Basic function utilizing Strings.
